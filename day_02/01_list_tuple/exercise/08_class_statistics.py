student_scores = [98, 75, 100, 86, 100, 3]

total_score = sum(student_scores)
number_of_scores = len(student_scores)

average_score = total_score // number_of_scores
print(f"Your average is {average_score}.")

# TODO: Print the average score